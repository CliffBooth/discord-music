[] rate limit:
===
[] cache per-route ratelimit
    need to have a cache of whether you can do reqeust

    bucket_id => (hits_available, reset_timestamp)

    when you do a requst, get this from cache and check if you have hits_available(), if not - return error. If available, after request update cache with new value from header X-Ratelimit-Remaining

    maybe we also need mapping of endpoint(or action) -> bucket_id

    if there is no bucket_id for our actoin, we make request and then cache action->bucket_id

    how to check hists_available():
        1. If reset_after happened, then reset hits available, return true
        2. If it didn't happen, check whether hits_available > 0 return true
        3. if hits_available <= 0, return false

[] Also, there is global ratelimit of 50 rps. this bot unlikely will do 50rps, but still it is good to implement this logic.

==